services:
  ${service_name}:
    image: ${REGISTRY_HOST}/${service_name}/${CI_COMMIT_REF_SLUG:-local}
    build:
      context: ../src
      additional_contexts:
        docker: ../docker
      dockerfile: ../docker/ruby/Dockerfile
    ports:
      - "7000:7000"
