services:
  ${service_name}:
    image: ${REGISTRY_HOST}/${service_name}/${CI_COMMIT_BRANCH}
    build:
      context: ../src
      additional_contexts:
        docker: ../docker
      dockerfile: ../docker/ruby/Dockerfile
    ports:
      - "7000:7000"
