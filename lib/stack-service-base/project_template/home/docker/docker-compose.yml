services:
  ${service_name}:
    image: ${REGISTRY_HOST}/${service_name}
    build:
      context: ../src
      additional_contexts:
        docker: ../docker
      dockerfile: ../docker/ruby/Dockerfile
    ports:
      - "8000:8000"
